# Plan de repaso 48h PSP

## Día 1 - Base segura

1. Procesos: ejercicios 01 al 09.
2. Hilos básicos: ejercicios 10 al 15.
3. Productor/consumidor: ejercicios 16 y 17.
4. Rehacer a mano sin mirar:
   - contador sincronizado,
   - proceso `wc` con entrada desde Java,
   - servidor TCP eco simple.

## Día 2 - Lo que más cae

1. TCP multihilo: ejercicios 27, 28, 30 y 31.
2. UDP: ejercicios 23, 24 y 25.
3. Mixtos: ejercicios 32, 33, 34 y 35.
4. Rehacer a mano sin mirar:
   - servidor TCP multihilo,
   - productor/consumidor con `wait/notifyAll`,
   - padre que lanza hijo y le manda líneas.

## Prioridad si vas justo de tiempo

1. `ProcessBuilder` + redirecciones.
2. `Thread`, `Runnable`, `join`.
3. `synchronized`, `wait`, `notifyAll`.
4. TCP servidor/cliente.
5. TCP servidor multihilo.
6. UDP básico.
7. Mezclas: socket + hilo + proceso.
